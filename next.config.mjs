/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "http://localhost:3000",
    "https://chidchanun.online",
    "https://www.chidchanun.online",
  ],
};

export default nextConfig;
